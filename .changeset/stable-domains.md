---
"@rstreamlabs/tunnels": patch
"@rstreamlabs/react": patch
---

Add stable domain, published port, and upstream TLS fields to tunnel and stream summary schemas, and prefer the stable published authority when parsing WebTTY servers.
