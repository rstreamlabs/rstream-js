---
"@rstreamlabs/runtime": minor
---

Add the Node.js tunnel runtime SDK with bytestream tunnel creation, private bytestream dialing, HTTP server serving, WebSocket upgrade support, shared runtime configuration resolution, and real-engine E2E coverage.
