---
"@rstreamlabs/tunnels": minor
---

Expose friendly WebTTY server identifiers from the tunnels parser — `tunnel_name`, `target`, and `rstream_url`, plus the tunnel's `status`, `publish`, `workspace_id`, and `project_id` — derived to match the canonical server-inventory model so every surface resolves the same target.
